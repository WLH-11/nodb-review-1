//You can delete the app registry if you would like, but do not remove anything from this file that you see below. This handles the rendering of your application.
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));